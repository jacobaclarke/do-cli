env:
  NAME: jacob
  IMAGE: ${REGISTRY}
tasks:
  get:
    cmd: echo ${REGISTRY}{{ARGS}}
  cat: 
    cmd: cat do.yaml
  echo:
    cmd: echo hello
  i:
    cmd: cargo install --path .
  nest:
    cmd: doit echo
  