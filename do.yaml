env:
  NAME: jacob
  IMAGE: ${REGISTRY}
tasks:
  get:
    cmd: echo ${REGISTRY}$1
  cat: 
    cmd: cat do.yaml
  echo:
    cmd: echo $HELLO
    env: 
      HELLO: jacob
  i:
    cmd: cargo install --path .
  nest:
    cmd: doit echo
    env:
      HELLO: travis
  
  